# Set these in Vercel project settings (not committed to git)

# Full HTTPS URL of the Railway backend service
VITE_API_URL=https://your-project.up.railway.app

# WSS URL of the Railway backend (same domain, wss:// prefix)
VITE_WS_URL=wss://your-project.up.railway.app
